# git-scanline — Test Configuration
# Copy this file to .env (git-ignored) and set TEST_REPO_PATH to any local git repository.
# The integration tests read git history from this repo but never modify it.
#
# Usage:
#   cp .env.example .env
#   # Then edit .env and set the path below:

TEST_REPO_PATH=/Users/yourname/path/to/any-git-repo
